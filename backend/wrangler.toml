name = "price-tracker-api"
main = "workers/index.js"
compatibility_date = "2024-01-01"

[vars]
SUPABASE_URL = "https://qdwhkfqvrlpfmoyviebg.supabase.co"

# Secrets added via: wrangler secret put SUPABASE_KEY
# Secrets added via: wrangler secret put GEMINI_API_KEY

[triggers]
crons = ["0 */1 * * *"]  # Run price checks every hour
